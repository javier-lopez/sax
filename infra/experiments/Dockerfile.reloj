FROM python:3.12-slim
RUN apt-get update && apt-get install -y --no-install-recommends libsndfile1 libcairo2 ffmpeg fonts-dejavu-core && rm -rf /var/lib/apt/lists/*
RUN pip install --no-cache-dir numpy scipy librosa soundfile verovio cairosvg pillow
WORKDIR /work
